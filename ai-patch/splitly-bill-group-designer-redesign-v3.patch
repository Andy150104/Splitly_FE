diff --git a/src/app/(protected)/bills/[billId]/page.tsx b/src/app/(protected)/bills/[billId]/page.tsx
index 60cd815..48b5834 100644
--- a/src/app/(protected)/bills/[billId]/page.tsx
+++ b/src/app/(protected)/bills/[billId]/page.tsx
@@ -1,11 +1,4 @@
-import {
-  Banknote,
-  CalendarClock,
-  CheckCircle2,
-  Clock3,
-  Sparkles,
-  UsersRound,
-} from "lucide-react";
+import { CalendarClock, CheckCircle2, Clock3, UsersRound } from "lucide-react";
 import { notFound } from "next/navigation";
 
 import { PageHeader } from "@/components/common/page-header";
@@ -40,40 +33,18 @@ export default async function BillDetailPage({
     .filter((member) => (member.remainingAmount ?? 0) > 0 && member.memberId)
     .map((member) => member.memberId!);
 
-  const metrics = [
-    {
-      label: "Tổng hóa đơn",
-      value: formatCurrency(bill.totalAmount, currency),
-      helper: "Giá trị hóa đơn hiện tại",
-      icon: Banknote,
-    },
-    {
-      label: "Đã thu",
-      value: formatCurrency(bill.collectedAmount, currency),
-      helper: "Đã ghi nhận thanh toán",
-      icon: CheckCircle2,
-    },
-    {
-      label: "Còn phải thu",
-      value: formatCurrency(bill.remainingAmount, currency),
-      helper: "Cần tiếp tục thu thêm",
-      icon: Clock3,
-    },
-    {
-      label: "Đã thanh toán",
-      value: `${bill.paidMemberCount ?? 0}/${members.length} người`,
-      helper: "Tiến độ theo thành viên",
-      icon: UsersRound,
-    },
-  ];
+  const completion = Math.min(100, Math.max(0, bill.completionPercentage ?? 0));
+  const totalText = formatCurrency(bill.totalAmount, currency);
+  const collectedText = formatCurrency(bill.collectedAmount, currency);
+  const remainingText = formatCurrency(bill.remainingAmount, currency);
 
   return (
-    <div className="animate-page-enter space-y-6 lg:space-y-7">
+    <div className="animate-page-enter space-y-5 lg:space-y-6">
       <PageHeader
         title={bill.title || "Chi tiết hóa đơn"}
         description={
           bill.description ||
-          "Theo dõi tiến độ thanh toán, số tiền còn lại và lịch sử thanh toán của từng thành viên."
+          "Theo dõi tiến độ thanh toán và số tiền của từng thành viên."
         }
         actions={
           bill.isOwner ? (
@@ -86,178 +57,109 @@ export default async function BillDetailPage({
         }
       />
 
-      <section className="grid gap-4 xl:grid-cols-[minmax(0,1.1fr)_340px]">
-        <Card className="overflow-hidden">
-          <CardContent className="from-primary/[0.06] via-card to-card relative p-6 sm:p-7">
-            <div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-primary/90 via-primary/50 to-transparent" />
-            <div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between">
-              <div className="space-y-4">
-                <div className="flex flex-wrap items-center gap-2">
-                  <BillStatusBadge status={bill.status} />
-                  {bill.isOwner ? <Badge variant="default">Bạn là chủ hóa đơn</Badge> : null}
-                  {(bill.overdueMemberCount ?? 0) > 0 ? (
-                    <Badge variant="destructive">
-                      {bill.overdueMemberCount} người quá hạn
-                    </Badge>
-                  ) : null}
-                </div>
-                <div className="space-y-1.5">
-                  <h2 className="text-2xl font-semibold tracking-tight sm:text-[1.95rem]">
-                    {bill.title || "Hóa đơn chưa đặt tên"}
-                  </h2>
-                  <p className="text-muted-foreground max-w-2xl text-sm leading-6 sm:text-base">
-                    {bill.description ||
-                      "Theo dõi tiến độ thanh toán của từng thành viên trong hóa đơn này."}
-                  </p>
-                </div>
-              </div>
-              <div className="min-w-[220px] rounded-3xl border border-border/75 bg-background/78 p-5 backdrop-blur">
-                <p className="text-muted-foreground text-sm">Tổng giá trị hóa đơn</p>
-                <p className="money mt-2 text-3xl font-bold tracking-[-0.04em]">
-                  {formatCurrency(bill.totalAmount, currency)}
-                </p>
-                <div className="text-muted-foreground mt-4 flex items-center gap-2 text-sm">
-                  <CalendarClock className="size-4" />
-                  Hạn thanh toán: {formatDate(bill.dueDate)}
-                </div>
-              </div>
+      <Card className="overflow-hidden">
+        <CardContent className="p-0">
+          <div className="flex flex-col gap-4 border-b border-border/75 px-5 py-4 sm:flex-row sm:items-center sm:justify-between sm:px-6">
+            <div className="flex flex-wrap items-center gap-2">
+              <BillStatusBadge status={bill.status} />
+              {bill.isOwner ? <Badge variant="default">Chủ hóa đơn</Badge> : null}
+              {(bill.overdueMemberCount ?? 0) > 0 ? (
+                <Badge variant="destructive">
+                  {bill.overdueMemberCount} người quá hạn
+                </Badge>
+              ) : null}
             </div>
-          </CardContent>
-        </Card>
-
-        <Card>
-          <CardHeader>
-            <CardTitle className="flex items-center gap-2 text-base">
-              <Sparkles className="text-primary size-4" />
-              Tóm tắt nhanh
-            </CardTitle>
-            <CardDescription>
-              Bức tranh tổng quan về hóa đơn để theo dõi nhanh.
-            </CardDescription>
-          </CardHeader>
-          <CardContent className="space-y-4">
-            <SummaryRow label="Tiền tệ" value={currency} />
-            <SummaryRow label="Số thành viên" value={`${members.length} người`} />
-            <SummaryRow label="Đã thu" value={formatCurrency(bill.collectedAmount, currency)} />
-            <SummaryRow label="Còn lại" value={formatCurrency(bill.remainingAmount, currency)} />
-            <SummaryRow label="Hạn thanh toán" value={formatDate(bill.dueDate)} />
-          </CardContent>
-        </Card>
-      </section>
+            <div className="text-muted-foreground flex items-center gap-2 text-sm">
+              <CalendarClock className="size-4" />
+              <span>Hạn thanh toán: {formatDate(bill.dueDate)}</span>
+            </div>
+          </div>
 
-      <section className="grid gap-4 sm:grid-cols-2 2xl:grid-cols-4">
-        {metrics.map((metric) => (
-          <Card
-            key={metric.label}
-            className="group hover:border-primary/25 hover:-translate-y-0.5"
-          >
-            <CardContent className="p-5">
-              <div className="mb-3 flex items-center justify-between gap-3">
-                <p className="text-muted-foreground text-sm">{metric.label}</p>
-                <div className="bg-primary/10 text-primary grid size-9 place-items-center rounded-2xl">
-                  <metric.icon className="size-4 transition-transform duration-200 group-hover:scale-110" />
-                </div>
-              </div>
-              <p className="money text-xl font-bold tracking-tight sm:text-2xl">
-                {metric.value}
+          <div className="grid lg:grid-cols-[minmax(240px,0.9fr)_minmax(0,1.6fr)]">
+            <div className="border-b border-border/75 px-5 py-5 sm:px-6 lg:border-r lg:border-b-0 lg:py-6">
+              <p className="text-muted-foreground text-sm">Tổng hóa đơn</p>
+              <p
+                className="money mt-2 break-words text-[clamp(1.9rem,3.2vw,2.75rem)] font-bold tracking-[-0.04em]"
+                title={totalText}
+              >
+                {totalText}
               </p>
-              <p className="text-muted-foreground mt-2 text-xs leading-5">
-                {metric.helper}
+              <p className="text-muted-foreground mt-2 text-sm">
+                {members.length} thành viên tham gia
               </p>
-            </CardContent>
-          </Card>
-        ))}
-      </section>
-
-      <div className="grid gap-6 xl:grid-cols-[minmax(0,1.12fr)_320px]">
-        <Card>
-          <CardHeader>
-            <CardTitle>Thành viên và thanh toán</CardTitle>
-            <CardDescription>
-              Theo dõi tình trạng thanh toán của từng thành viên và ghi nhận thanh toán thủ công khi cần.
-            </CardDescription>
-          </CardHeader>
-          <CardContent>
-            <BillMemberList
-              billId={billId}
-              currency={currency}
-              members={members}
-              canManage={Boolean(bill.isOwner)}
-            />
-          </CardContent>
-        </Card>
-
-        <div className="space-y-6">
-          <Card>
-            <CardContent className="p-5 sm:p-6">
-              <div className="mb-3 flex items-center justify-between gap-3">
-                <div>
-                  <span className="text-sm font-semibold">Tiến độ thu tiền</span>
-                  <p className="money text-muted-foreground mt-1 text-xs">
-                    {formatCurrency(bill.collectedAmount, currency)} /{" "}
-                    {formatCurrency(bill.totalAmount, currency)}
-                  </p>
-                </div>
-                <strong className="money text-sm">
-                  {Math.round(bill.completionPercentage ?? 0)}%
-                </strong>
-              </div>
-              <Progress value={bill.completionPercentage ?? 0} className="h-2.5" />
-            </CardContent>
-          </Card>
+            </div>
 
-          <Card>
-            <CardHeader>
-              <CardTitle className="text-base">Nhận định thanh toán</CardTitle>
-              <CardDescription>
-                Trạng thái hiện tại của hóa đơn này.
-              </CardDescription>
-            </CardHeader>
-            <CardContent className="space-y-4 text-sm leading-6">
-              <div className="bg-muted/60 rounded-2xl border border-border/70 p-4">
-                {(bill.remainingAmount ?? 0) > 0 ? (
-                  <p>
-                    Còn <strong>{formatCurrency(bill.remainingAmount, currency)}</strong> cần thu từ{" "}
-                    <strong>{members.filter((member) => (member.remainingAmount ?? 0) > 0).length} thành viên</strong>.
-                  </p>
-                ) : (
-                  <p>
-                    Tất cả các thành viên đã hoàn tất thanh toán. Hóa đơn đang ở trạng thái hoàn tất.
-                  </p>
-                )}
-              </div>
-              <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-1">
-                <MiniInsight
-                  label="Tỷ lệ hoàn thành"
-                  value={`${Math.round(bill.completionPercentage ?? 0)}%`}
+            <div className="px-5 py-5 sm:px-6 lg:py-6">
+              <div className="grid gap-4 sm:grid-cols-3">
+                <BillMetric
+                  icon={CheckCircle2}
+                  label="Đã thu"
+                  value={collectedText}
                 />
-                <MiniInsight
-                  label="Người đã thanh toán"
-                  value={`${bill.paidMemberCount ?? 0}/${members.length}`}
+                <BillMetric icon={Clock3} label="Còn lại" value={remainingText} />
+                <BillMetric
+                  icon={UsersRound}
+                  label="Đã thanh toán"
+                  value={`${bill.paidMemberCount ?? 0}/${members.length} người`}
                 />
               </div>
-            </CardContent>
-          </Card>
-        </div>
-      </div>
-    </div>
-  );
-}
 
-function SummaryRow({ label, value }: { label: string; value: string }) {
-  return (
-    <div className="flex items-center justify-between gap-4 border-b border-border/70 pb-3 last:border-none last:pb-0">
-      <span className="text-muted-foreground text-sm">{label}</span>
-      <span className="text-right text-sm font-medium">{value}</span>
+              <div className="mt-5 border-t border-border/70 pt-4">
+                <div className="mb-2 flex items-center justify-between gap-3 text-sm">
+                  <span className="font-medium">Tiến độ thu tiền</span>
+                  <span className="money font-semibold">{Math.round(completion)}%</span>
+                </div>
+                <Progress value={completion} className="h-2.5" />
+                <div className="text-muted-foreground mt-2 flex flex-col gap-1 text-xs sm:flex-row sm:items-center sm:justify-between">
+                  <span>{collectedText} đã thu</span>
+                  <span>{remainingText} còn lại</span>
+                </div>
+              </div>
+            </div>
+          </div>
+        </CardContent>
+      </Card>
+
+      <Card className="overflow-hidden">
+        <CardHeader className="border-b border-border/75 pb-4">
+          <CardTitle className="text-lg">Thành viên và thanh toán</CardTitle>
+          <CardDescription className="mt-1.5">
+            Theo dõi trạng thái và lịch sử thanh toán của từng người.
+          </CardDescription>
+        </CardHeader>
+        <CardContent className="p-4 sm:p-5">
+          <BillMemberList
+            billId={billId}
+            currency={currency}
+            members={members}
+            canManage={Boolean(bill.isOwner)}
+          />
+        </CardContent>
+      </Card>
     </div>
   );
 }
 
-function MiniInsight({ label, value }: { label: string; value: string }) {
+function BillMetric({
+  icon: Icon,
+  label,
+  value,
+}: {
+  icon: typeof CheckCircle2;
+  label: string;
+  value: string;
+}) {
   return (
-    <div className="rounded-2xl border border-border/75 bg-muted/40 px-4 py-3">
-      <p className="text-muted-foreground text-xs uppercase tracking-wide">{label}</p>
-      <p className="money mt-1.5 text-lg font-semibold">{value}</p>
+    <div className="min-w-0">
+      <div className="flex items-center gap-2">
+        <Icon className="text-primary size-4" />
+        <p className="text-muted-foreground text-xs font-medium uppercase tracking-wide">
+          {label}
+        </p>
+      </div>
+      <p className="money mt-2 truncate text-lg font-semibold" title={value}>
+        {value}
+      </p>
     </div>
   );
 }
diff --git a/src/app/(protected)/groups/[groupId]/page.tsx b/src/app/(protected)/groups/[groupId]/page.tsx
index 336db20..3acc09e 100644
--- a/src/app/(protected)/groups/[groupId]/page.tsx
+++ b/src/app/(protected)/groups/[groupId]/page.tsx
@@ -1,11 +1,4 @@
-import {
-  ArrowRight,
-  FileText,
-  FolderLock,
-  ReceiptText,
-  UsersRound,
-  WalletCards,
-} from "lucide-react";
+import { FileText, UsersRound, WalletCards } from "lucide-react";
 import Link from "next/link";
 import { notFound } from "next/navigation";
 
@@ -38,43 +31,19 @@ export default async function GroupDetailPage({
 
   const members = group.members ?? [];
   const bills = group.bills ?? [];
-  const totalValue = bills.reduce((sum, bill) => sum + (bill.totalAmount ?? 0), 0);
+  const activeMembers = members.filter((member) => member.status === "Active").length;
   const publishedBills = bills.filter((bill) => bill.status !== "Draft").length;
-  const statusVariant =
-    group.status === "Active"
-      ? "success"
-      : group.status === "Closed"
-        ? "secondary"
-        : "warning";
-
-  const stats = [
-    {
-      label: "Thành viên",
-      value: `${members.length}`,
-      helper: `${members.filter((member) => member.status === "Active").length} đang hoạt động`,
-      icon: UsersRound,
-    },
-    {
-      label: "Hóa đơn",
-      value: `${bills.length}`,
-      helper: `${publishedBills} đã công bố`,
-      icon: ReceiptText,
-    },
-    {
-      label: "Tổng giá trị",
-      value: formatCurrency(totalValue, bills[0]?.currency ?? "VND"),
-      helper: "Tổng tiền của các hóa đơn trong nhóm",
-      icon: WalletCards,
-    },
-  ];
+  const totalValue = bills.reduce((sum, bill) => sum + (bill.totalAmount ?? 0), 0);
+  const currency = bills[0]?.currency ?? "VND";
+  const formattedTotalValue = formatCurrency(totalValue, currency);
 
   return (
-    <div className="animate-page-enter space-y-6 lg:space-y-7">
+    <div className="animate-page-enter space-y-5 lg:space-y-6">
       <PageHeader
         title={group.name || "Chi tiết nhóm"}
         description={
           group.description ||
-          "Quản lý thành viên, lời mời và các hóa đơn thuộc nhóm này."
+          "Quản lý thành viên và các hóa đơn thuộc nhóm này."
         }
         actions={
           group.isOwner && group.status === "Active" ? (
@@ -83,109 +52,85 @@ export default async function GroupDetailPage({
         }
       />
 
-      <section className="grid gap-4 md:grid-cols-3">
-        <Card className="md:col-span-2 overflow-hidden">
-          <CardContent className="from-primary/[0.06] via-card to-card relative p-6 sm:p-7">
-            <div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-primary/90 via-primary/45 to-transparent" />
-            <div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between">
-              <div className="space-y-4">
-                <div className="flex flex-wrap items-center gap-2">
-                  <Badge variant={statusVariant}>{group.status}</Badge>
-                  {group.isOwner ? <Badge variant="default">Bạn là chủ nhóm</Badge> : null}
-                </div>
-                <div className="space-y-2">
-                  <h2 className="text-2xl font-semibold tracking-tight">
-                    Không gian chung cho thành viên và hóa đơn
-                  </h2>
-                  <p className="text-muted-foreground max-w-2xl text-sm leading-6 sm:text-base">
-                    Theo dõi danh sách thành viên, hóa đơn đã tạo và trạng thái của cả nhóm trong một màn hình duy nhất.
-                  </p>
-                </div>
-              </div>
-              <div className="grid grid-cols-3 gap-3 self-stretch lg:min-w-[360px]">
-                {stats.map((stat) => (
-                  <div
-                    key={stat.label}
-                    className="bg-background/72 border-border/70 rounded-2xl border px-4 py-4 backdrop-blur"
-                  >
-                    <div className="mb-3 flex items-center justify-between gap-2">
-                      <p className="text-muted-foreground text-xs font-medium tracking-wide uppercase">
-                        {stat.label}
-                      </p>
-                      <stat.icon className="text-primary size-4" />
-                    </div>
-                    <p className="money text-lg font-semibold tracking-tight sm:text-xl">
-                      {stat.value}
-                    </p>
-                    <p className="text-muted-foreground mt-1 text-xs leading-5">
-                      {stat.helper}
-                    </p>
-                  </div>
-                ))}
-              </div>
+      <Card className="overflow-hidden">
+        <CardContent className="p-0">
+          <div className="flex flex-col gap-4 border-b border-border/75 px-5 py-4 sm:flex-row sm:items-center sm:justify-between sm:px-6">
+            <div className="flex flex-wrap items-center gap-2">
+              <Badge variant={group.status === "Active" ? "success" : "secondary"}>
+                {group.status}
+              </Badge>
+              {group.isOwner ? <Badge variant="default">Chủ nhóm</Badge> : null}
             </div>
-          </CardContent>
-        </Card>
+            <p className="text-muted-foreground text-sm">
+              {group.status === "Active"
+                ? "Nhóm đang hoạt động và có thể nhận thành viên, hóa đơn mới."
+                : "Nhóm hiện không nhận thêm thành viên hoặc hóa đơn mới."}
+            </p>
+          </div>
 
-        <Card>
-          <CardHeader>
-            <CardTitle className="flex items-center gap-2 text-base">
-              <FolderLock className="text-primary size-4" />
-              Tóm tắt nhanh
-            </CardTitle>
-            <CardDescription>
-              Các thông tin cốt lõi để quản lý nhóm dễ hơn trên cả desktop và điện thoại.
-            </CardDescription>
-          </CardHeader>
-          <CardContent className="space-y-4">
-            <SummaryRow label="Trạng thái" value={group.status ?? "-"} />
-            <SummaryRow label="Số thành viên" value={`${members.length} người`} />
-            <SummaryRow label="Số hóa đơn" value={`${bills.length} hóa đơn`} />
-            <SummaryRow
-              label="Quyền của bạn"
-              value={group.isOwner ? "Chủ nhóm" : "Thành viên"}
+          <div className="grid divide-y divide-border/75 sm:grid-cols-3 sm:divide-x sm:divide-y-0">
+            <OverviewMetric
+              icon={UsersRound}
+              label="Thành viên"
+              value={`${members.length}`}
+              helper={`${activeMembers} đang hoạt động`}
             />
-          </CardContent>
-        </Card>
-      </section>
+            <OverviewMetric
+              icon={FileText}
+              label="Hóa đơn"
+              value={`${bills.length}`}
+              helper={`${publishedBills} đã công bố`}
+            />
+            <OverviewMetric
+              icon={WalletCards}
+              label="Tổng giá trị"
+              value={formattedTotalValue}
+              helper="Tất cả hóa đơn trong nhóm"
+              title={formattedTotalValue}
+            />
+          </div>
+        </CardContent>
+      </Card>
 
-      <section className="grid gap-6 xl:grid-cols-[minmax(0,1.15fr)_minmax(340px,0.85fr)]">
+      <section className="grid gap-5 xl:grid-cols-[minmax(0,1.25fr)_minmax(340px,0.75fr)]">
         <Card className="overflow-hidden">
-          <CardHeader className="border-b border-border/80 pb-4">
+          <CardHeader className="border-b border-border/75 pb-4">
             <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
               <div>
-                <CardTitle className="flex items-center gap-2 text-lg">
-                  <UsersRound className="text-primary size-5" />
-                  Thành viên nhóm
-                </CardTitle>
-                <CardDescription className="mt-2">
-                  Hiển thị vai trò, trạng thái và cho phép thêm hoặc xóa thành viên nhanh chóng.
+                <CardTitle className="text-lg">Thành viên</CardTitle>
+                <CardDescription className="mt-1.5">
+                  {members.length} thành viên trong nhóm.
                 </CardDescription>
               </div>
-              <Badge variant="secondary">{members.length} thành viên</Badge>
+              {group.isOwner && group.status === "Active" ? (
+                <AddGroupMembers groupId={groupId} />
+              ) : null}
             </div>
           </CardHeader>
-          <CardContent className="space-y-5 p-4 sm:p-5">
-            <div className="space-y-3">
-              {members.length ? (
-                members.map((member) => (
+
+          <CardContent className="p-0">
+            {members.length ? (
+              <div className="divide-y divide-border/70">
+                {members.map((member) => (
                   <div
                     key={member.memberId}
-                    className="hover:border-primary/25 hover:bg-muted/35 flex flex-col gap-4 rounded-2xl border border-border/75 px-4 py-4 transition-colors sm:flex-row sm:items-center"
+                    className="flex flex-col gap-3 px-5 py-4 transition-colors hover:bg-muted/25 sm:flex-row sm:items-center sm:px-6"
                   >
-                    <div className="bg-primary/10 text-primary grid size-11 shrink-0 place-items-center rounded-full text-sm font-bold">
+                    <div className="bg-primary/10 text-primary grid size-10 shrink-0 place-items-center rounded-full text-xs font-bold">
                       {(member.name || member.email || "U")
                         .slice(0, 2)
                         .toUpperCase()}
                     </div>
+
                     <div className="min-w-0 flex-1">
-                      <p className="truncate text-sm font-semibold sm:text-base">
+                      <p className="truncate text-sm font-semibold">
                         {member.name || member.email}
                       </p>
-                      <p className="text-muted-foreground truncate text-sm">
+                      <p className="text-muted-foreground truncate text-xs sm:text-sm">
                         {member.email}
                       </p>
                     </div>
+
                     <div className="flex flex-wrap items-center gap-2 sm:justify-end">
                       <Badge
                         variant={
@@ -198,6 +143,7 @@ export default async function GroupDetailPage({
                       >
                         {member.role === "Owner" ? "Chủ nhóm" : member.status}
                       </Badge>
+
                       {group.isOwner && member.role !== "Owner" && member.memberId ? (
                         <RemoveGroupMember
                           groupId={groupId}
@@ -207,89 +153,70 @@ export default async function GroupDetailPage({
                       ) : null}
                     </div>
                   </div>
-                ))
-              ) : (
-                <p className="text-muted-foreground py-8 text-center text-sm">
-                  Nhóm chưa có thành viên nào.
-                </p>
-              )}
-            </div>
-
-            {group.isOwner && group.status === "Active" ? (
-              <div className="bg-muted/40 rounded-2xl border border-border/70 p-4 sm:p-5">
-                <div className="mb-4 flex items-start gap-3">
-                  <div className="bg-primary/10 text-primary grid size-10 shrink-0 place-items-center rounded-2xl">
-                    <UsersRound className="size-4" />
-                  </div>
-                  <div>
-                    <p className="font-semibold">Mời thêm thành viên</p>
-                    <p className="text-muted-foreground mt-1 text-sm leading-6">
-                      Gửi lời mời nhanh cho nhiều người chỉ trong một thao tác.
-                    </p>
-                  </div>
-                </div>
-                <AddGroupMembers groupId={groupId} />
+                ))}
               </div>
-            ) : null}
+            ) : (
+              <EmptySection
+                title="Chưa có thành viên"
+                description="Thêm thành viên để bắt đầu chia hóa đơn trong nhóm."
+              />
+            )}
           </CardContent>
         </Card>
 
         <Card className="overflow-hidden">
-          <CardHeader className="border-b border-border/80 pb-4">
+          <CardHeader className="border-b border-border/75 pb-4">
             <div className="flex items-center justify-between gap-3">
               <div>
-                <CardTitle className="flex items-center gap-2 text-lg">
-                  <FileText className="text-primary size-5" />
-                  Hóa đơn của nhóm
-                </CardTitle>
-                <CardDescription className="mt-2">
-                  Các hóa đơn mới nhất được tạo trong nhóm này.
+                <CardTitle className="text-lg">Hóa đơn</CardTitle>
+                <CardDescription className="mt-1.5">
+                  Các hóa đơn thuộc nhóm này.
                 </CardDescription>
               </div>
               <Badge variant="secondary">{bills.length}</Badge>
             </div>
           </CardHeader>
-          <CardContent className="space-y-3 p-4 sm:p-5">
+
+          <CardContent className="p-0">
             {bills.length ? (
-              bills.map((bill) => (
-                <Link
-                  key={bill.billId}
-                  href={`/bills/${bill.billId}`}
-                  className="group hover:border-primary/30 hover:bg-muted/35 flex items-center justify-between gap-4 rounded-2xl border border-border/75 px-4 py-4 transition-colors"
-                >
-                  <div className="min-w-0 flex-1">
-                    <p className="truncate text-sm font-semibold sm:text-base">
-                      {bill.title}
-                    </p>
-                    <div className="mt-2 flex flex-wrap items-center gap-2">
-                      <BillStatusBadge status={bill.status} />
-                    </div>
-                  </div>
-                  <div className="flex items-center gap-3 pl-2">
-                    <div className="text-right">
-                      <p className="money text-sm font-semibold sm:text-base">
-                        {formatCurrency(bill.totalAmount, bill.currency ?? "VND")}
-                      </p>
-                      <p className="text-muted-foreground text-xs">
-                        Chi tiết hóa đơn
-                      </p>
-                    </div>
-                    <ArrowRight className="text-muted-foreground group-hover:text-primary size-4 transition-colors" />
-                  </div>
-                </Link>
-              ))
-            ) : (
-              <div className="flex min-h-[220px] flex-col items-center justify-center gap-3 rounded-2xl border border-dashed border-border/80 px-6 text-center">
-                <div className="bg-muted text-muted-foreground grid size-12 place-items-center rounded-2xl">
-                  <ReceiptText className="size-5" />
-                </div>
-                <div>
-                  <p className="font-medium">Nhóm chưa có hóa đơn</p>
-                  <p className="text-muted-foreground mt-1 text-sm leading-6">
-                    Khi tạo hóa đơn mới, chúng sẽ xuất hiện ở đây để mọi người cùng theo dõi.
-                  </p>
-                </div>
+              <div className="divide-y divide-border/70">
+                {bills.map((bill) => {
+                  const amount = formatCurrency(
+                    bill.totalAmount,
+                    bill.currency ?? "VND",
+                  );
+
+                  return (
+                    <Link
+                      key={bill.billId}
+                      href={`/bills/${bill.billId}`}
+                      className="group flex items-center justify-between gap-4 px-5 py-4 transition-colors hover:bg-muted/25 sm:px-6"
+                    >
+                      <div className="min-w-0 flex-1">
+                        <p className="truncate text-sm font-semibold">
+                          {bill.title || "Hóa đơn chưa đặt tên"}
+                        </p>
+                        <div className="mt-2">
+                          <BillStatusBadge status={bill.status} />
+                        </div>
+                      </div>
+                      <div className="min-w-0 max-w-[45%] text-right">
+                        <p className="money truncate text-sm font-semibold" title={amount}>
+                          {amount}
+                        </p>
+                        <p className="text-muted-foreground mt-1 text-xs group-hover:text-foreground">
+                          Xem chi tiết
+                        </p>
+                      </div>
+                    </Link>
+                  );
+                })}
               </div>
+            ) : (
+              <EmptySection
+                title="Chưa có hóa đơn"
+                description="Hóa đơn mới của nhóm sẽ xuất hiện tại đây."
+              />
             )}
           </CardContent>
         </Card>
@@ -298,11 +225,49 @@ export default async function GroupDetailPage({
   );
 }
 
-function SummaryRow({ label, value }: { label: string; value: string }) {
+function OverviewMetric({
+  icon: Icon,
+  label,
+  value,
+  helper,
+  title,
+}: {
+  icon: typeof UsersRound;
+  label: string;
+  value: string;
+  helper: string;
+  title?: string;
+}) {
+  return (
+    <div className="min-w-0 px-5 py-4 sm:px-6 sm:py-5">
+      <div className="flex items-center gap-2">
+        <Icon className="text-primary size-4" />
+        <p className="text-muted-foreground text-xs font-medium uppercase tracking-wide">
+          {label}
+        </p>
+      </div>
+      <p
+        className="money mt-2 truncate text-xl font-semibold tracking-tight sm:text-2xl"
+        title={title}
+      >
+        {value}
+      </p>
+      <p className="text-muted-foreground mt-1 text-xs">{helper}</p>
+    </div>
+  );
+}
+
+function EmptySection({
+  title,
+  description,
+}: {
+  title: string;
+  description: string;
+}) {
   return (
-    <div className="flex items-center justify-between gap-4 border-b border-border/70 pb-3 last:border-none last:pb-0">
-      <span className="text-muted-foreground text-sm">{label}</span>
-      <span className="text-right text-sm font-medium">{value}</span>
+    <div className="px-6 py-12 text-center">
+      <p className="text-sm font-medium">{title}</p>
+      <p className="text-muted-foreground mt-1 text-sm">{description}</p>
     </div>
   );
 }
diff --git a/src/app/globals.css b/src/app/globals.css
index 4464905..c21b881 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -126,9 +126,9 @@ select {
   animation: dialog-overlay-in var(--motion-base) var(--ease-out) both;
 }
 
-.animate-dialog-content {
-  transform-origin: 50% 30%;
-  animation: dialog-content-in var(--motion-slow) var(--ease-out) both;
+.animate-dialog-panel {
+  transform-origin: 50% 100%;
+  animation: dialog-panel-in var(--motion-slow) var(--ease-out) both;
 }
 
 @keyframes page-enter {
@@ -182,14 +182,14 @@ select {
   }
 }
 
-@keyframes dialog-content-in {
+@keyframes dialog-panel-in {
   from {
     opacity: 0;
-    transform: translate(-50%, calc(-50% + 18px)) scale(0.96);
+    transform: translateY(22px) scale(0.985);
   }
   to {
     opacity: 1;
-    transform: translate(-50%, -50%) scale(1);
+    transform: translateY(0) scale(1);
   }
 }
 
diff --git a/src/components/ui/dialog.tsx b/src/components/ui/dialog.tsx
index 6078564..a2a0bfd 100644
--- a/src/components/ui/dialog.tsx
+++ b/src/components/ui/dialog.tsx
@@ -18,7 +18,7 @@ export const DialogOverlay = React.forwardRef<
   <DialogPrimitive.Overlay
     ref={ref}
     className={cn(
-      "animate-dialog-overlay fixed inset-0 z-50 bg-slate-950/45 backdrop-blur-[2px] dark:bg-slate-950/65",
+      "animate-dialog-overlay fixed inset-0 z-50 bg-slate-950/45 backdrop-blur-[2px] dark:bg-slate-950/70",
       className,
     )}
     {...props}
@@ -34,17 +34,23 @@ export const DialogContent = React.forwardRef<
     <DialogOverlay />
     <DialogPrimitive.Content
       ref={ref}
-      className={cn(
-        "bg-card text-card-foreground animate-dialog-content fixed top-[50%] left-[50%] z-50 grid w-[calc(100vw-1.5rem)] max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 rounded-[1.4rem] border border-border/80 p-6 shadow-[0_18px_70px_rgb(15_23_42/0.24)] duration-200 sm:w-full sm:p-7 dark:shadow-[0_24px_80px_rgb(2_6_23/0.55)]",
-        className,
-      )}
+      className="fixed inset-x-0 bottom-0 z-50 w-full outline-none sm:inset-x-auto sm:bottom-auto sm:left-1/2 sm:top-1/2 sm:w-[calc(100vw-2rem)] sm:max-w-md sm:-translate-x-1/2 sm:-translate-y-1/2"
       {...props}
     >
-      {children}
-      <DialogPrimitive.Close className="text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:ring-ring absolute top-4 right-4 rounded-full p-2 transition-colors focus-visible:outline-none focus-visible:ring-2">
-        <X className="size-4" />
-        <span className="sr-only">Đóng</span>
-      </DialogPrimitive.Close>
+      <div
+        className={cn(
+          "animate-dialog-panel bg-card text-card-foreground relative max-h-[88dvh] overflow-y-auto rounded-t-[1.6rem] border border-border/80 shadow-[0_-12px_48px_rgb(15_23_42/0.18)] sm:rounded-[1.35rem] sm:shadow-[0_24px_80px_rgb(15_23_42/0.22)] dark:shadow-[0_-12px_56px_rgb(2_6_23/0.55)] sm:dark:shadow-[0_24px_80px_rgb(2_6_23/0.6)]",
+          className,
+        )}
+      >
+        <div className="p-5 pb-[max(1.25rem,env(safe-area-inset-bottom))] sm:p-6">
+          {children}
+        </div>
+        <DialogPrimitive.Close className="text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:ring-ring absolute right-4 top-4 rounded-full p-2 transition-colors focus-visible:outline-none focus-visible:ring-2">
+          <X className="size-4" />
+          <span className="sr-only">Đóng</span>
+        </DialogPrimitive.Close>
+      </div>
     </DialogPrimitive.Content>
   </DialogPortal>
 ));
@@ -54,7 +60,7 @@ export function DialogHeader({
   className,
   ...props
 }: React.HTMLAttributes<HTMLDivElement>) {
-  return <div className={cn("flex flex-col gap-2", className)} {...props} />;
+  return <div className={cn("flex flex-col gap-2 pr-8", className)} {...props} />;
 }
 
 export function DialogFooter({
@@ -64,7 +70,7 @@ export function DialogFooter({
   return (
     <div
       className={cn(
-        "flex flex-col-reverse gap-2 sm:flex-row sm:justify-end",
+        "mt-6 flex flex-col gap-2 sm:flex-row sm:justify-end",
         className,
       )}
       {...props}
@@ -78,7 +84,7 @@ export const DialogTitle = React.forwardRef<
 >(({ className, ...props }, ref) => (
   <DialogPrimitive.Title
     ref={ref}
-    className={cn("text-xl font-semibold tracking-tight", className)}
+    className={cn("text-lg font-semibold tracking-tight sm:text-xl", className)}
     {...props}
   />
 ));
diff --git a/src/features/groups/components/group-actions.tsx b/src/features/groups/components/group-actions.tsx
index 73a0f42..a91bb6c 100644
--- a/src/features/groups/components/group-actions.tsx
+++ b/src/features/groups/components/group-actions.tsx
@@ -14,91 +14,112 @@ import {
   DialogHeader,
   DialogTitle,
 } from "@/components/ui/dialog";
-import { Input } from "@/components/ui/input";
 import { Label } from "@/components/ui/label";
+import { Textarea } from "@/components/ui/textarea";
 import { bffFetch } from "@/lib/http/browser-http-client";
 
 export function AddGroupMembers({ groupId }: { groupId: string }) {
+  const [open, setOpen] = useState(false);
   const [value, setValue] = useState("");
   const [pending, setPending] = useState(false);
   const router = useRouter();
 
-  const emailCount = useMemo(
+  const emails = useMemo(
     () =>
       value
         .split(/[\n,;]+/)
         .map((item) => item.trim())
-        .filter(Boolean).length,
+        .filter(Boolean),
     [value],
   );
 
   return (
-    <form
-      className="space-y-3"
-      onSubmit={async (event) => {
-        event.preventDefault();
-        const emails = value
-          .split(/[\n,;]+/)
-          .map((item) => item.trim())
-          .filter(Boolean);
-        if (!emails.length) return;
+    <>
+      <Button size="sm" onClick={() => setOpen(true)}>
+        <UserPlus2 className="size-4" />
+        Thêm thành viên
+      </Button>
+
+      <Dialog open={open} onOpenChange={setOpen}>
+        <DialogContent>
+          <form
+            onSubmit={async (event) => {
+              event.preventDefault();
+              if (!emails.length) return;
 
-        setPending(true);
-        try {
-          const result = await bffFetch<{
-            addedCount?: number;
-            pendingAccountCount?: number;
-          }>(`/api/groups/${groupId}/members`, {
-            method: "POST",
-            body: JSON.stringify({ emails }),
-          });
-          toast.success(`Đã thêm ${result.addedCount ?? emails.length} thành viên`);
-          setValue("");
-          router.refresh();
-        } catch (error) {
-          toast.error(
-            error instanceof Error
-              ? error.message
-              : "Không thể thêm thành viên.",
-          );
-        } finally {
-          setPending(false);
-        }
-      }}
-    >
-      <div className="grid gap-3 lg:grid-cols-[1fr_auto] lg:items-end">
-        <div className="space-y-2">
-          <Label htmlFor="member-emails">Email thành viên</Label>
-          <Input
-            id="member-emails"
-            value={value}
-            onChange={(event) => setValue(event.target.value)}
-            placeholder="anna@example.com, minh@example.com"
-          />
-          <p className="text-muted-foreground text-xs leading-5">
-            Nhập nhiều email, ngăn cách bằng dấu phẩy hoặc xuống dòng.
-          </p>
-        </div>
-        <Button
-          isLoading={pending}
-          loadingText="Đang thêm…"
-          disabled={!emailCount}
-          className="w-full lg:w-auto"
-        >
-          <UserPlus2 className="size-4" />
-          Thêm thành viên
-        </Button>
-      </div>
+              setPending(true);
+              try {
+                const result = await bffFetch<{
+                  addedCount?: number;
+                  pendingAccountCount?: number;
+                }>(`/api/groups/${groupId}/members`, {
+                  method: "POST",
+                  body: JSON.stringify({ emails }),
+                });
+                toast.success(
+                  `Đã thêm ${result.addedCount ?? emails.length} thành viên`,
+                );
+                setValue("");
+                setOpen(false);
+                router.refresh();
+              } catch (error) {
+                toast.error(
+                  error instanceof Error
+                    ? error.message
+                    : "Không thể thêm thành viên.",
+                );
+              } finally {
+                setPending(false);
+              }
+            }}
+          >
+            <DialogHeader>
+              <DialogTitle>Thêm thành viên</DialogTitle>
+              <DialogDescription>
+                Nhập một hoặc nhiều email. Email chưa có tài khoản sẽ được tạo ở trạng thái Pending.
+              </DialogDescription>
+            </DialogHeader>
 
-      {emailCount ? (
-        <div className="bg-muted/70 text-muted-foreground flex flex-col gap-1 rounded-2xl border border-border/70 px-4 py-3 text-sm sm:flex-row sm:items-center sm:justify-between">
-          <span>Sẵn sàng mời {emailCount} email.</span>
-          <span className="font-medium">
-            Tài khoản chưa tồn tại sẽ được tạo ở trạng thái Pending.
-          </span>
-        </div>
-      ) : null}
-    </form>
+            <div className="mt-5 space-y-2">
+              <Label htmlFor="group-member-emails">Email thành viên</Label>
+              <Textarea
+                id="group-member-emails"
+                value={value}
+                onChange={(event) => setValue(event.target.value)}
+                placeholder={"anna@example.com\nminh@example.com"}
+                className="min-h-28"
+                autoFocus
+              />
+              <div className="text-muted-foreground flex items-center justify-between gap-3 text-xs">
+                <span>Phân tách bằng dấu phẩy, chấm phẩy hoặc xuống dòng.</span>
+                <span className="shrink-0 font-medium">
+                  {emails.length} email
+                </span>
+              </div>
+            </div>
+
+            <DialogFooter>
+              <Button
+                type="button"
+                variant="outline"
+                onClick={() => setOpen(false)}
+              >
+                Hủy
+              </Button>
+              <Button
+                type="submit"
+                isLoading={pending}
+                loadingText="Đang thêm…"
+                disabled={!emails.length}
+              >
+                <UserPlus2 className="size-4" />
+                Thêm thành viên
+              </Button>
+            </DialogFooter>
+          </form>
+        </DialogContent>
+      </Dialog>
+    </>
   );
 }
 
@@ -131,26 +152,23 @@ export function RemoveGroupMember({
       <Dialog open={open} onOpenChange={setOpen}>
         <DialogContent>
           <DialogHeader>
-            <DialogTitle>Xóa thành viên khỏi nhóm?</DialogTitle>
+            <DialogTitle>Xóa thành viên?</DialogTitle>
             <DialogDescription>
               {memberName ? (
                 <>
-                  Bạn sắp xóa{" "}
-                  <strong className="text-foreground">{memberName}</strong> khỏi
-                  nhóm này.
+                  <strong className="text-foreground">{memberName}</strong> sẽ
+                  bị xóa khỏi nhóm. Lịch sử hóa đơn đã phát sinh vẫn được giữ lại.
                 </>
               ) : (
-                "Bạn sắp xóa một thành viên khỏi nhóm này."
-              )}{" "}
-              Lịch sử hóa đơn vẫn được backend giữ lại.
+                "Thành viên sẽ bị xóa khỏi nhóm. Lịch sử hóa đơn đã phát sinh vẫn được giữ lại."
+              )}
             </DialogDescription>
           </DialogHeader>
 
-          <div className="bg-destructive/[0.055] border-destructive/15 flex items-start gap-3 rounded-2xl border px-4 py-3 text-sm">
+          <div className="border-destructive/15 bg-destructive/[0.055] mt-5 flex items-start gap-3 rounded-xl border px-4 py-3 text-sm">
             <AlertTriangle className="text-destructive mt-0.5 size-4 shrink-0" />
             <p className="text-muted-foreground leading-6">
-              Thành viên sẽ không còn xuất hiện trong danh sách nhóm hiện tại sau
-              khi thao tác hoàn tất.
+              Thao tác này ảnh hưởng đến danh sách thành viên hiện tại của nhóm.
             </p>
           </div>
 
@@ -210,22 +228,22 @@ export function CloseGroupButton({ groupId }: { groupId: string }) {
       <Dialog open={open} onOpenChange={setOpen}>
         <DialogContent>
           <DialogHeader>
-            <DialogTitle>Đóng nhóm này?</DialogTitle>
+            <DialogTitle>Đóng nhóm?</DialogTitle>
             <DialogDescription>
-              Khi đóng nhóm, bạn sẽ không thể thêm thành viên mới hoặc tạo hóa đơn
-              mới cho nhóm này.
+              Sau khi đóng, nhóm không thể nhận thêm thành viên hoặc hóa đơn mới. Dữ liệu hiện tại vẫn được giữ nguyên.
             </DialogDescription>
           </DialogHeader>
 
-          <div className="bg-muted/75 rounded-2xl border border-border/80 p-4">
-            <p className="text-sm font-semibold">
-              Điều gì sẽ xảy ra sau khi đóng nhóm?
-            </p>
-            <ul className="text-muted-foreground mt-3 space-y-2 text-sm leading-6">
-              <li>• Nhóm chuyển sang trạng thái đóng.</li>
-              <li>• Không thể nhận thêm thành viên hoặc hóa đơn mới.</li>
-              <li>• Lịch sử thành viên và hóa đơn hiện tại vẫn được giữ nguyên.</li>
-            </ul>
+          <div className="bg-muted/65 mt-5 rounded-xl border border-border/80 px-4 py-4">
+            <div className="flex items-start gap-3">
+              <AlertTriangle className="text-muted-foreground mt-0.5 size-4 shrink-0" />
+              <div className="space-y-1.5 text-sm leading-6">
+                <p className="font-medium">Nhóm sẽ chuyển sang trạng thái đóng.</p>
+                <p className="text-muted-foreground">
+                  Thành viên và hóa đơn cũ vẫn có thể được xem lại.
+                </p>
+              </div>
+            </div>
           </div>
 
           <DialogFooter>
@@ -256,7 +274,7 @@ export function CloseGroupButton({ groupId }: { groupId: string }) {
                 }
               }}
             >
-              Xác nhận đóng nhóm
+              Đóng nhóm
             </Button>
           </DialogFooter>
         </DialogContent>
