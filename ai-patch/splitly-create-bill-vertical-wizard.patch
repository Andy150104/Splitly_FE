diff --git a/src/app/(protected)/bills/new/page.tsx b/src/app/(protected)/bills/new/page.tsx
index 991df73..a9de924 100644
--- a/src/app/(protected)/bills/new/page.tsx
+++ b/src/app/(protected)/bills/new/page.tsx
@@ -12,7 +12,7 @@ export default async function NewBillPage() {
   ]);
 
   return (
-    <div className="flex flex-col gap-4">
+    <div className="flex flex-col gap-3">
       <PageHeader
         compact
         title="Tạo hóa đơn"
diff --git a/src/features/bills/components/create-bill/create-bill-flow.tsx b/src/features/bills/components/create-bill/create-bill-flow.tsx
index 715eb04..5bbebcd 100644
--- a/src/features/bills/components/create-bill/create-bill-flow.tsx
+++ b/src/features/bills/components/create-bill/create-bill-flow.tsx
@@ -33,7 +33,12 @@ import { formatCurrency } from "@/lib/formatters/currency";
 import { bffFetch } from "@/lib/http/browser-http-client";
 import { cn } from "@/lib/utils";
 
-const steps = ["Thông tin", "Người tham gia", "Chia tiền", "Xác nhận"];
+const steps = [
+  { label: "Thông tin", description: "Nhập thông tin khoản chi" },
+  { label: "Người tham gia", description: "Chọn người cùng chia" },
+  { label: "Chia tiền", description: "Chọn cách phân bổ" },
+  { label: "Xác nhận", description: "Kiểm tra và hoàn tất" },
+] as const;
 const emailsFrom = (value?: string) =>
   Array.from(
     new Set(
@@ -172,59 +177,115 @@ export function CreateBillFlow({
           );
         },
       )}
-      className="mx-auto w-full max-w-4xl"
+      className="mx-auto w-full max-w-5xl"
     >
-      <ol
-        className="mb-4 grid grid-cols-4 gap-2"
-        aria-label="Tiến độ tạo hóa đơn"
-      >
-        {steps.map((label, index) => {
-          const completed = index < step;
-          const active = index === step;
+      <Card className="border-border/80 overflow-hidden shadow-[0_14px_42px_rgb(15_23_42/0.055)] dark:shadow-[0_18px_50px_rgb(0_0_0/0.22)]">
+        <div className="lg:grid lg:grid-cols-[220px_minmax(0,1fr)]">
+          <aside className="border-border/70 bg-muted/20 hidden border-r lg:block dark:bg-muted/[0.08]">
+            <div className="sticky top-20 p-5">
+              <p className="text-muted-foreground text-[11px] font-bold tracking-[0.16em] uppercase">
+                Tiến độ
+              </p>
+              <ol className="mt-5" aria-label="Tiến độ tạo hóa đơn">
+                {steps.map((item, index) => {
+                  const completed = index < step;
+                  const active = index === step;
+
+                  return (
+                    <li
+                      key={item.label}
+                      className="relative flex gap-3 pb-7 last:pb-0"
+                    >
+                      {index < steps.length - 1 ? (
+                        <span
+                          aria-hidden="true"
+                          className={cn(
+                            "bg-border absolute top-8 bottom-0 left-[15px] w-px transition-colors duration-200",
+                            completed && "bg-primary/45",
+                          )}
+                        />
+                      ) : null}
+                      <span
+                        className={cn(
+                          "bg-background text-muted-foreground border-border relative z-10 grid size-8 shrink-0 place-items-center rounded-full border text-xs font-bold shadow-[0_1px_2px_rgb(15_23_42/0.05)] transition-[border-color,background-color,color,box-shadow] duration-200 dark:bg-background/70",
+                          completed &&
+                            "border-primary/25 bg-primary/10 text-primary",
+                          active &&
+                            "border-primary bg-primary text-primary-foreground shadow-[0_0_0_4px_color-mix(in_oklab,var(--primary)_11%,transparent)]",
+                        )}
+                      >
+                        {completed ? (
+                          <Check className="size-4" aria-hidden="true" />
+                        ) : (
+                          index + 1
+                        )}
+                      </span>
+                      <div className="min-w-0 pt-0.5">
+                        <p
+                          className={cn(
+                            "text-muted-foreground text-sm font-medium transition-colors",
+                            (completed || active) && "text-foreground",
+                            active && "font-semibold",
+                          )}
+                        >
+                          {item.label}
+                        </p>
+                        <p
+                          className={cn(
+                            "text-muted-foreground/70 mt-0.5 text-xs leading-4",
+                            active && "text-muted-foreground",
+                          )}
+                        >
+                          {item.description}
+                        </p>
+                      </div>
+                    </li>
+                  );
+                })}
+              </ol>
+            </div>
+          </aside>
 
-          return (
-            <li key={label} className="min-w-0">
+          <section className="min-w-0">
+            <div className="border-border/70 bg-muted/15 border-b px-4 py-3 lg:hidden dark:bg-muted/[0.06]">
+              <div className="flex items-center justify-between gap-3">
+                <div className="min-w-0">
+                  <p className="text-muted-foreground text-[11px] font-semibold tracking-wide uppercase">
+                    Bước {step + 1} / {steps.length}
+                  </p>
+                  <p className="mt-0.5 truncate text-sm font-semibold">
+                    {steps[step].label}
+                  </p>
+                </div>
+                <span className="bg-primary/10 text-primary rounded-full px-2.5 py-1 text-xs font-semibold">
+                  {Math.round(((step + 1) / steps.length) * 100)}%
+                </span>
+              </div>
               <div
-                className={cn(
-                  "border-border/60 bg-card/55 text-muted-foreground flex h-10 min-w-0 items-center gap-2 rounded-xl border px-2.5 transition-[border-color,background-color,box-shadow,color] duration-200",
-                  completed &&
-                    "border-primary/20 bg-primary/[0.055] text-foreground",
-                  active &&
-                    "border-primary/40 bg-card text-foreground shadow-[0_4px_14px_rgb(15_23_42/0.055),0_0_0_3px_color-mix(in_oklab,var(--primary)_7%,transparent)] dark:bg-card dark:shadow-[0_6px_18px_rgb(0_0_0/0.18),0_0_0_3px_color-mix(in_oklab,var(--primary)_10%,transparent)]",
-                )}
+                className="mt-3 grid grid-cols-4 gap-1.5"
+                aria-hidden="true"
               >
-                <span
-                  className={cn(
-                    "bg-muted text-muted-foreground grid size-5 shrink-0 place-items-center rounded-full text-[10px] font-bold transition-colors",
-                    completed && "bg-primary/15 text-primary",
-                    active && "bg-primary text-primary-foreground",
-                  )}
-                >
-                  {completed ? <Check className="size-3" /> : index + 1}
-                </span>
-                <span
-                  className={cn(
-                    "min-w-0 truncate text-[11px] font-medium sm:text-xs",
-                    active && "font-semibold",
-                  )}
-                >
-                  {label}
-                </span>
+                {steps.map((item, index) => (
+                  <span
+                    key={item.label}
+                    className={cn(
+                      "bg-muted h-1.5 rounded-full transition-colors duration-200",
+                      index <= step && "bg-primary",
+                    )}
+                  />
+                ))}
               </div>
-            </li>
-          );
-        })}
-      </ol>
-      <Card className="border-border/80 shadow-[0_12px_36px_rgb(15_23_42/0.04)] dark:shadow-[0_16px_44px_rgb(0_0_0/0.16)]">
-        <CardContent className="p-4 sm:p-5">
-          <div key={step} className="animate-step-in">
+            </div>
+
+            <CardContent className="p-4 sm:p-5 lg:p-6">
+              <div key={step} className="animate-step-in">
             {step === 0 ? (
-              <div className="space-y-4">
-                <div className="border-primary/10 bg-primary/[0.025] rounded-xl border px-3.5 py-2.5 dark:bg-primary/[0.045]">
-                  <h2 className="text-base font-semibold tracking-tight">
+              <div className="space-y-5">
+                <div className="border-border/70 border-b pb-4">
+                  <h2 className="text-lg font-semibold tracking-tight">
                     Thông tin hóa đơn
                   </h2>
-                  <p className="text-muted-foreground mt-0.5 text-sm leading-5">
+                  <p className="text-muted-foreground mt-1 text-sm leading-5">
                     Cho mọi người biết khoản chi này là gì và khi nào cần thanh
                     toán.
                   </p>
@@ -334,10 +395,12 @@ export function CreateBillFlow({
             ) : null}
 
             {step === 1 ? (
-              <div className="space-y-4">
-                <div>
-                  <h2 className="text-lg font-semibold">Chọn người tham gia</h2>
-                  <p className="text-muted-foreground mt-1 text-sm">
+              <div className="space-y-5">
+                <div className="border-border/70 border-b pb-4">
+                  <h2 className="text-lg font-semibold tracking-tight">
+                    Chọn người tham gia
+                  </h2>
+                  <p className="text-muted-foreground mt-1 text-sm leading-5">
                     Thêm trực tiếp bằng email, hoặc chọn thành viên từ một nhóm
                     có sẵn.
                   </p>
@@ -463,10 +526,12 @@ export function CreateBillFlow({
             ) : null}
 
             {step === 2 ? (
-              <div className="space-y-4">
-                <div>
-                  <h2 className="text-lg font-semibold">Chọn cách chia</h2>
-                  <p className="text-muted-foreground mt-1 text-sm">
+              <div className="space-y-5">
+                <div className="border-border/70 border-b pb-4">
+                  <h2 className="text-lg font-semibold tracking-tight">
+                    Chọn cách chia
+                  </h2>
+                  <p className="text-muted-foreground mt-1 text-sm leading-5">
                     Chia đều hoặc nhập chính xác số tiền mỗi người cần trả.
                   </p>
                 </div>
@@ -548,12 +613,12 @@ export function CreateBillFlow({
             ) : null}
 
             {step === 3 ? (
-              <div className="space-y-4">
-                <div>
-                  <h2 className="text-lg font-semibold">
+              <div className="space-y-5">
+                <div className="border-border/70 border-b pb-4">
+                  <h2 className="text-lg font-semibold tracking-tight">
                     Kiểm tra trước khi tạo
                   </h2>
-                  <p className="text-muted-foreground mt-1 text-sm">
+                  <p className="text-muted-foreground mt-1 text-sm leading-5">
                     Bạn có thể quay lại để chỉnh sửa mà không mất dữ liệu.
                   </p>
                 </div>
@@ -600,34 +665,40 @@ export function CreateBillFlow({
                 </label>
               </div>
             ) : null}
-          </div>
-        </CardContent>
+              </div>
+            </CardContent>
+
+            <div className="border-border/70 bg-card/95 sticky bottom-0 z-10 flex items-center justify-between gap-3 border-t px-3 py-3 backdrop-blur-xl sm:px-5 lg:static lg:bg-card/80 lg:px-6 dark:bg-card/95">
+              <Button
+                type="button"
+                variant="ghost"
+                disabled={step === 0 || form.formState.isSubmitting}
+                onClick={() => setStep((current) => current - 1)}
+              >
+                <ArrowLeft className="size-4" />
+                Quay lại
+              </Button>
+              <div className="text-muted-foreground hidden text-xs sm:block lg:hidden">
+                {steps[step].label}
+              </div>
+              {step < 3 ? (
+                <Button type="button" onClick={next}>
+                  Tiếp tục <ArrowRight className="size-4" />
+                </Button>
+              ) : (
+                <Button
+                  type="submit"
+                  isLoading={form.formState.isSubmitting}
+                  loadingText="Đang tạo…"
+                >
+                  <Check className="size-4" />
+                  {values.publish ? "Tạo & công bố" : "Lưu bản nháp"}
+                </Button>
+              )}
+            </div>
+          </section>
+        </div>
       </Card>
-      <div className="border-border/70 bg-background/95 sticky bottom-3 z-10 mt-3 flex justify-between rounded-2xl border p-1.5 shadow-[0_10px_30px_rgb(15_23_42/0.10)] backdrop-blur-xl sm:static sm:border-0 sm:bg-transparent sm:p-0 sm:shadow-none dark:shadow-[0_12px_34px_rgb(0_0_0/0.28)]">
-        <Button
-          type="button"
-          variant="ghost"
-          disabled={step === 0 || form.formState.isSubmitting}
-          onClick={() => setStep((current) => current - 1)}
-        >
-          <ArrowLeft className="size-4" />
-          Quay lại
-        </Button>
-        {step < 3 ? (
-          <Button type="button" onClick={next}>
-            Tiếp tục <ArrowRight className="size-4" />
-          </Button>
-        ) : (
-          <Button
-            type="submit"
-            isLoading={form.formState.isSubmitting}
-            loadingText="Đang tạo…"
-          >
-            <Check className="size-4" />
-            {values.publish ? "Tạo & công bố" : "Lưu bản nháp"}
-          </Button>
-        )}
-      </div>
     </form>
   );
 }
